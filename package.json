{
  "name": "@nti/lib-ranges",
  "version": "1.9.0-alpha",
  "author": "nextthought",
  "license": "UNLICENSED",
  "description": "Content Anchor handling library",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "karma start --single-run --reporters dots,html,junit,coverage",
    "start": "make check",
    "prepack": "make check && npm test",
    "release": "./release.sh"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NextThought/nti.lib.ranges.git"
  },
  "publishConfig": {
    "registry": "https://npm.nextthought.com"
  },
  "dependencies": {
    "@nti/lib-anchors": "^1.0.0",
    "@nti/lib-dom": "^1.0.0",
    "@nti/util-logger": "^1.0.0"
  },
  "devDependencies": {
    "@nti/lib-scripts": "^2.12.4",
    "@nti/unittesting-clientside": "^2.2.2",
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/polyfill": "^7.0.0",
    "@babel/preset-env": "^7.0.0"
  }
}
